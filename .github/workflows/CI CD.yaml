name: My Test Workflow (Heshan) (Skyu Generated)
'on':
  push:
    branches:
      - master
  workflow_dispatch: {}
env:
  DOCKER_FILE_NAME: HELLO
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Start
        run: echo " Starting GitHub Action!"
